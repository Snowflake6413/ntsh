# nothing to see here....

```bash
docker build -t sussy-wussy .
docker run -d -p 6901:6901 -e VNC_PW=password sussy-wussy
```

Open https://localhost:6901 in your browser (user: `kasm_user`, password: `password`).
